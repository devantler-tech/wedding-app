name: 🎉 Release
on:
  push:
    branches: [main]
  workflow_dispatch: 

permissions: {}

jobs:
  release:
    uses: devantler-tech/actions/.github/workflows/create-release.yaml@9713532ad079498f12077b06b6cdf6e127f53773 # v9.0.10
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
